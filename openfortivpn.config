# VPN Settings
host = vpn.domain.tld
port = 443
username = <vpn-username>
password = <vpn-password>
trusted-cert = <cert hash>

# Additional VPN Settings

# Uncomment this if the VPN is not split tunnel, then you can add the routes you need manually.
#set-routes = 0
